[SERVICE]
    Flush 1
    Log_Level info

[INPUT]
    Name maces
    Tag maces
    # events exec,fork,exit
    events exec

[OUTPUT]
    Name stdout
    Match *
    Format json
    json_date_format iso8601
